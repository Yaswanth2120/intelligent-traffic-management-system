<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.traffic</groupId>
  <artifactId>traffic-platform</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>pom</packaging>

  <!--
    integration-tests is intentionally NOT listed here. Every service Dockerfile
    (feature-service/Dockerfile, decision-engine/Dockerfile, ...) COPYs this root
    pom.xml plus only the specific sibling pom.xml files it needs into the Maven
    reactor build inside the image; listing integration-tests as a module here
    would break those builds (the reactor would expect integration-tests/pom.xml
    to exist in the build context). integration-tests/pom.xml still declares
    this pom as its <parent> for dependency/version management, and is built
    standalone via `mvn -f integration-tests/pom.xml verify` (see docs/testing.md).
  -->
  <modules>
    <module>gateway-service</module>
    <module>feature-service</module>
    <module>decision-engine</module>
  </modules>

  <properties>
    <java.version>17</java.version>
    <maven.compiler.source>${java.version}</maven.compiler.source>
    <maven.compiler.target>${java.version}</maven.compiler.target>
    <spring.boot.version>3.3.2</spring.boot.version>
    <testcontainers.version>2.0.5</testcontainers.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.testcontainers</groupId>
        <artifactId>testcontainers-bom</artifactId>
        <version>${testcontainers.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring.boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.springframework.boot</groupId>
          <artifactId>spring-boot-maven-plugin</artifactId>
          <!--
            Without an explicit execution binding, spring-boot-maven-plugin does NOT
            repackage the jar into a runnable fat jar when a project doesn't inherit
            from spring-boot-starter-parent (this project only imports the
            spring-boot-dependencies BOM). Before this fix, `mvn package` on any
            service produced a ~20KB plain jar with no Main-Class manifest entry,
            so every service Dockerfile (`java -jar app.jar`) failed at container
            startup with "no main manifest attribute, in app.jar": caught by
            integration-tests/TrafficPipelineIT actually starting the containers.
          -->
          <executions>
            <execution>
              <goals>
                <goal>repackage</goal>
              </goals>
            </execution>
          </executions>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>3.2.5</version>
          <configuration>
            <argLine>-Dnet.bytebuddy.experimental=true</argLine>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-failsafe-plugin</artifactId>
          <version>3.2.5</version>
          <configuration>
            <argLine>-Dnet.bytebuddy.experimental=true</argLine>
          </configuration>
          <executions>
            <execution>
              <goals>
                <goal>integration-test</goal>
                <goal>verify</goal>
              </goals>
            </execution>
          </executions>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>
